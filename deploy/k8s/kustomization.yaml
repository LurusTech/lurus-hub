apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lurus-system

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pdb.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd

images:
  - name: ghcr.io/lurustech/lurus-api
    newTag: main
