# Development Progress Archive / 开发进度归档

> Archived: 2026-02-06
> Contains entries from 2026-01-20 to 2026-02-03
> Active log: ../process.md

---

## 2026-02-03: Stories 1.2/1.3/1.4 — Production Deployment

Deployed multi-tenant V2 API to K3s with Zitadel OIDC auth.
- DB backup: `/tmp/lurusapi_pre_v2_20260203.dump` on master (115K)
- 3 new tables confirmed: `tenants`, `user_identity_mapping`, `tenant_configs` (already migrated)
- Updated deployment.yaml: OIDC_* → ZITADEL_* env vars, SESSION_SECRET from secret
- Fixed Service selector mismatch (added `app.kubernetes.io/managed-by: argocd` label to pod template)
- Created initial tenant: slug=`lurus`, org_id=`356204220778610952`
Verification:
```
/api/status → 200, V2 login → 302 to auth.lurus.cn, JWKS refreshed (2 keys), V1 panel → 200
```
Remaining: Full OAuth callback E2E needs browser test (PKCE params pending next image build).

---

## 2026-02-03: Story 2.3 — Controller Layer Test Coverage

2 new test files + 7 appended files, ~50 subtests passing, 0 new failures.
- `helpers_test.go` (13 subtests): hasRole, maskSingleKey, maskKey, maskRedemptionKey
- `topup_creem_test.go` (9 subtests): generateCreemSignature, verifyCreemSignature
- V2 test files: user(3), token(4), log(2), channel(5), billing(3), redemption(4), admin(4) subtests
- Fixed SQLite index collision in SetupV2TestRouter, exported InitCol()
Verification: `go test -v ./internal/server/controller/... → PASS`
Note: 4 pre-existing CN/EN message mismatch failures unrelated.

---

## 2026-02-02: Story 2.1 — Service Layer Test Coverage

7 test files, 187 tests PASS for 6 service files in `internal/biz/service/`.
Coverage: group.go 100%, channel.go 93.1%, channel_select 100%, notify-limit 100%, quota.go 100%.
Package-level 12.8% expected (heavy DB-dependent functions need integration infra).
Verification: `go test -v ./internal/biz/service/... → 187 PASS, 0 FAIL`

---

## 2026-02-02: Story 2.2 — Relay Adaptor Test Coverage

5 new test files, ~100+ subtests PASS. Covered BaseAdaptor, OpenAI, Claude, Gemini adaptors.
- Claude: stop reason mapping, stream events, tool choice
- Gemini: thinking budget clamping, image conversion, quality mapping
- OpenAI: o-series rewrites, gpt-5 stripping, suffix parsing
Verification: `go test -v ./internal/biz/relay/... → PASS`

---

## 2026-02-02: BMAD Improvement Plan (P0-P3) — 14 Items

- P0: v1 tenant isolation, session secure flag, fmt.Println→slog (3 items)
- P1: service layer extraction, v1/v2 dedup, composite indexes, goroutine lifecycle (4 items)
- P2: BaseAdaptor framework, test coverage (97 tests), centralized config, slog logging (4 items)
- P3: HA deployment, v1 deprecation, observability decision records (3 items)
Verification: `go build/vet/test → PASS (97 tests)`

---

## 2026-01-25 ~ 2026-02-01: Multi-Tenant SaaS Planning

Completed 6-phase multi-tenant transformation plan.
- Zitadel OIDC integration design (org→tenant, project→app, user→mapping)
- Tenant isolation: shared DB + tenant_id + GORM plugin auto-inject
- API versioning: /api/* (v1 compat), /api/v2/:slug/* (multi-tenant), /api/v2/admin/*
- 26 v2 endpoints implemented, graceful shutdown, go-redis v9 upgrade
Infrastructure: Zitadel at auth.lurus.cn, K3s 4-node cluster, ArgoCD GitOps.
